
<%= form_with(model: @coursecohort) do |form| %>
  Course: <%= form.select :course_id, options_for_select(@courses, @coursecohort.course_id) %><br />
    Cohort: <%= form.select :cohort_id, options_for_select(@cohorts, @coursecohort.cohort_id) %><br />
<%= form.submit %>
<% end %>

